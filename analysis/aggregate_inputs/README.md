# Aggregate inputs

These de-identified aggregate tables and figures are the minimal checked inputs needed to rebuild the selected manuscript presentation artifacts without distributing record-level VA data. Fresh outputs under `outputs/agreement_analysis/` take precedence when the assembler is run.
